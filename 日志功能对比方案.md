# .NET 版本 vs .NET Framework 版本日志功能对比方案

## 概述

本文档详细对比了表格 OCR 项目在 .NET 8.0 版本和 .NET Framework 4.7.2 版本中的日志功能实现差异，并提供了优化建议。

## 1. 日志架构对比

### .NET 8.0 版本（自定义日志系统）

**架构特点：**
- 使用自定义的 `ILogger` 接口和 `FileLogger` 实现
- 简单的文件日志记录器
- 无依赖外部日志框架

**核心组件：**
```csharp
// 自定义接口
public interface ILogger : IDisposable
{
    void Log(LogLevel level, string message, Exception exception = null);
}

// 自定义日志级别
public enum LogLevel
{
    Debug, Info, Warning, Error
}

// 文件日志实现
public class FileLogger : ILogger, IDisposable
```

**依赖包：**
- 无额外日志相关 NuGet 包
- 仅使用 .NET 内置功能

### .NET Framework 4.7.2 版本（Microsoft.Extensions.Logging）

**架构特点：**
- 使用 Microsoft.Extensions.Logging 标准日志框架
- 控制台日志输出
- 标准化的日志接口

**核心组件：**
```csharp
// 使用标准接口
using Microsoft.Extensions.Logging;
private static ILogger<Program> logger;

// 服务配置
services.AddLogging(builder =>
{
    builder.AddConsole();
    builder.SetMinimumLevel(LogLevel.Information);
});
```

**依赖包：**
- Microsoft.Extensions.Logging (3.1.32)
- Microsoft.Extensions.Logging.Console (3.1.32)
- Microsoft.Extensions.DependencyInjection (3.1.32)

## 2. 功能特性对比

| 特性 | .NET 8.0 版本 | .NET Framework 4.7.2 版本 |
|------|---------------|---------------------------|
| **日志接口** | 自定义 ILogger | Microsoft.Extensions.Logging.ILogger |
| **日志级别** | 自定义枚举 (4个级别) | 标准枚举 (6个级别) |
| **输出目标** | 文件 | 控制台 |
| **线程安全** | ✅ (lock机制) | ✅ (框架保证) |
| **异常处理** | ✅ 详细堆栈跟踪 | ✅ 标准异常记录 |
| **时间戳** | ✅ 自定义格式 | ✅ 标准格式 |
| **依赖注入** | ❌ 手动管理 | ✅ 完全集成 |
| **配置灵活性** | ❌ 硬编码 | ✅ 配置文件支持 |
| **扩展性** | ❌ 有限 | ✅ 丰富的提供程序 |

## 3. 代码实现对比

### 3.1 日志记录方式

**.NET 8.0 版本：**
```csharp
// 手动创建和管理
var logger = new FileLogger("logs/app.log");
logger.Log(LogLevel.Info, "应用程序启动");
logger.Log(LogLevel.Error, "发生错误", exception);
logger.Dispose();
```

**.NET Framework 4.7.2 版本：**
```csharp
// 依赖注入获取
logger = serviceProvider.GetService<ILogger<Program>>();
logger.LogInformation("应用程序启动");
logger.LogError(exception, "程序执行过程中发生未处理的异常");
```

### 3.2 配置方式

**.NET 8.0 版本：**
```csharp
// 硬编码配置
services.AddSingleton<ILogger>(provider => 
    new FileLogger("logs/application.log"));
```

**.NET Framework 4.7.2 版本：**
```csharp
// 灵活配置
services.AddLogging(builder =>
{
    builder.AddConsole();
    builder.SetMinimumLevel(LogLevel.Information);
});
```

## 4. 优缺点分析

### .NET 8.0 版本优缺点

**优点：**
- ✅ 轻量级，无外部依赖
- ✅ 完全控制日志格式和行为
- ✅ 文件持久化存储
- ✅ 简单易理解

**缺点：**
- ❌ 功能有限，缺乏高级特性
- ❌ 不支持多种输出目标
- ❌ 缺乏配置灵活性
- ❌ 不符合 .NET 生态标准
- ❌ 难以与第三方工具集成

### .NET Framework 4.7.2 版本优缺点

**优点：**
- ✅ 标准化接口，符合 .NET 生态
- ✅ 丰富的配置选项
- ✅ 支持多种日志提供程序
- ✅ 完美的依赖注入集成
- ✅ 易于扩展和维护

**缺点：**
- ❌ 增加了项目依赖
- ❌ 当前只输出到控制台
- ❌ 需要额外的配置工作

## 5. 改进建议

### 5.1 .NET Framework 版本改进方案

**问题：** 当前只有控制台输出，缺少文件日志

**解决方案：** 添加文件日志提供程序

```csharp
// 1. 添加 NuGet 包
// Microsoft.Extensions.Logging.File 或 NLog.Extensions.Logging

// 2. 配置文件日志
services.AddLogging(builder =>
{
    builder.AddConsole();
    builder.AddFile("logs/app-{Date}.log"); // 如果使用文件提供程序
    builder.SetMinimumLevel(LogLevel.Information);
});
```

### 5.2 统一日志方案建议

**推荐方案：** 在 .NET Framework 版本基础上添加 NLog

**理由：**
- NLog 完全兼容 .NET Framework 4.7.2
- 可与 Microsoft.Extensions.Logging 无缝集成
- 提供丰富的配置选项和输出目标
- 高性能和稳定性

**实施步骤：**

1. **添加 NuGet 包：**
```xml
<PackageReference Include="NLog" Version="5.2.8" />
<PackageReference Include="NLog.Extensions.Logging" Version="5.3.8" />
```

2. **创建 NLog 配置文件 (nlog.config)：**
```xml
<?xml version="1.0" encoding="utf-8" ?>
<nlog xmlns="http://www.nlog-project.org/schemas/NLog.xsd"
      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance">
  <targets>
    <target xsi:type="File" name="fileTarget"
            fileName="logs/app-${shortdate}.log"
            layout="${longdate} [${level:uppercase=true}] ${logger} - ${message} ${exception:format=tostring}" />
    <target xsi:type="Console" name="consoleTarget"
            layout="${time} [${level:uppercase=true}] ${message}" />
  </targets>
  <rules>
    <logger name="*" minlevel="Info" writeTo="fileTarget,consoleTarget" />
  </rules>
</nlog>
```

3. **更新服务配置：**
```csharp
services.AddLogging(builder =>
{
    builder.ClearProviders();
    builder.AddNLog();
    builder.SetMinimumLevel(LogLevel.Information);
});
```

## 6. 迁移建议

### 6.1 短期方案（保持现状）
- .NET Framework 版本继续使用 Microsoft.Extensions.Logging
- 添加文件日志提供程序解决持久化问题
- 保持两个版本的功能一致性

### 6.2 长期方案（统一架构）
- 将 .NET 8.0 版本也迁移到 Microsoft.Extensions.Logging
- 使用 NLog 作为统一的日志实现
- 建立统一的日志配置和格式标准

## 7. 总结

.NET Framework 4.7.2 版本采用的 Microsoft.Extensions.Logging 方案更加标准化和可扩展，但需要补充文件日志功能。建议采用 NLog 作为统一的日志解决方案，既能保持标准化接口，又能提供丰富的功能特性。

**推荐优先级：**
1. **高优先级：** 为 .NET Framework 版本添加文件日志功能
2. **中优先级：** 统一两个版本的日志架构
3. **低优先级：** 添加高级日志功能（如结构化日志、日志聚合等）