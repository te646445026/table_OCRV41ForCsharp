# .NET 8.0 到 .NET Framework 4.7.2 迁移文档

## 📋 项目概述

### 源项目信息
- **项目名称**: table_OCRV41ForCsharp_net
- **当前框架**: .NET 8.0
- **项目类型**: 控制台应用程序
- **主要功能**: 腾讯云OCR表格识别、Word文档自动化处理

### 目标项目信息
- **目标框架**: .NET Framework 4.7.2
- **项目类型**: 控制台应用程序
- **兼容性要求**: Windows平台

## 🎯 迁移目标

1. 将现有.NET 8.0项目直接迁移到.NET Framework 4.7.2
2. **保持原有代码逻辑完全不变**
3. 仅进行必要的框架兼容性调整
4. 确保功能完全一致

## 📁 项目结构分析

### 核心文件清单
```
table_OCRV41ForCsharp_net/
├── Program.cs                          # 主程序入口
├── TencentOcrService.cs               # 腾讯云OCR服务
├── TencentOcrParser.cs                # OCR结果解析器
├── KeyService.cs                      # 密钥管理服务
├── PathService.cs                     # 路径管理服务
├── GetFileContentAsBase64Service.cs   # 文件Base64编码服务
├── Logger.cs                          # 日志记录器
├── HolidayService.cs                  # 假期服务
├── OcrResult.cs                       # OCR结果模型
├── OcrServiceException.cs             # OCR异常类
├── PathMessage.cs                     # 路径消息模型
├── KEY.cs                             # 密钥模型
├── 接口文件 (I*.cs)                   # 各种接口定义
├── 测试文件 (*Test.cs)                # 单元测试
└── 配置文件 (*.json)                  # 配置文件
```

## 🔧 详细迁移步骤

### 第一阶段：项目文件配置

#### 1.1 创建新的项目文件

直接复制现有的 .NET Framework 项目结构，创建传统格式的 `.csproj` 文件：

```xml
<?xml version="1.0" encoding="utf-8"?>
<Project ToolsVersion="15.0" xmlns="http://schemas.microsoft.com/developer/msbuild/2003">
  <Import Project="$(MSBuildExtensionsPath)\$(MSBuildToolsVersion)\Microsoft.Common.props" Condition="Exists('$(MSBuildExtensionsPath)\$(MSBuildToolsVersion)\Microsoft.Common.props')" />
  
  <PropertyGroup>
    <Configuration Condition=" '$(Configuration)' == '' ">Debug</Configuration>
    <Platform Condition=" '$(Platform)' == '' ">AnyCPU</Platform>
    <ProjectGuid>{新的GUID}</ProjectGuid>
    <OutputType>Exe</OutputType>
    <RootNamespace>table_OCRV41ForCsharp_net</RootNamespace>
    <AssemblyName>table_OCRV41ForCsharp_net</AssemblyName>
    <TargetFrameworkVersion>v4.7.2</TargetFrameworkVersion>
    <FileAlignment>512</FileAlignment>
    <AutoGenerateBindingRedirects>true</AutoGenerateBindingRedirects>
    <Deterministic>true</Deterministic>
  </PropertyGroup>
  
  <!-- 编译配置 -->
  <PropertyGroup Condition=" '$(Configuration)|$(Platform)' == 'Debug|AnyCPU' ">
    <PlatformTarget>AnyCPU</PlatformTarget>
    <DebugSymbols>true</DebugSymbols>
    <DebugType>full</DebugType>
    <Optimize>false</Optimize>
    <OutputPath>bin\Debug\</OutputPath>
    <DefineConstants>DEBUG;TRACE</DefineConstants>
    <ErrorReport>prompt</ErrorReport>
    <WarningLevel>4</WarningLevel>
  </PropertyGroup>
  
  <PropertyGroup Condition=" '$(Configuration)|$(Platform)' == 'Release|AnyCPU' ">
    <PlatformTarget>AnyCPU</PlatformTarget>
    <DebugType>pdbonly</DebugType>
    <Optimize>true</Optimize>
    <OutputPath>bin\Release\</OutputPath>
    <DefineConstants>TRACE</DefineConstants>
    <ErrorReport>prompt</ErrorReport>
    <WarningLevel>4</WarningLevel>
  </PropertyGroup>
  
  <!-- 引用配置将在后续步骤中添加 -->
</Project>
```

#### 1.2 创建 packages.config

直接使用与原有.NET 8.0项目相同的包，但调整为.NET Framework兼容版本：

```xml
<?xml version="1.0" encoding="utf-8"?>
<packages>
  <!-- 保持与原项目相同的核心依赖 -->
  <package id="Newtonsoft.Json" version="13.0.3" targetFramework="net472" />
  <package id="NPOI" version="2.7.1" targetFramework="net472" />
  
  <!-- 依赖注入 - 使用兼容版本 -->
  <package id="Microsoft.Extensions.DependencyInjection" version="3.1.32" targetFramework="net472" />
  <package id="Microsoft.Extensions.Configuration" version="3.1.32" targetFramework="net472" />
  
  <!-- 假期服务 - 保持原版本 -->
  <package id="Nager.Date" version="2.8.3" targetFramework="net472" />
  
  <!-- 必要的系统依赖 -->
  <package id="System.Memory" version="4.5.5" targetFramework="net472" />
  <package id="System.Threading.Tasks.Extensions" version="4.5.4" targetFramework="net472" />
</packages>
```

### 第二阶段：代码直接迁移

#### 2.1 Program.cs 最小化修改

**仅进行必要的语法调整**：
- 添加显式 using 语句
- 移除可空引用类型注解
- 保持所有原有逻辑不变

**修改原则**：
```csharp
// 1. 在文件顶部添加所有必要的 using 语句
using System;
using System.Collections;
using System.IO;
using System.Threading;
using System.Threading.Tasks;
using System.Windows.Forms;
using Microsoft.Extensions.DependencyInjection;
using NPOI.XWPF.UserModel;

// 2. 添加命名空间声明
namespace table_OCRV41ForCsharp_net
{
    // 3. 保持原有的 Program 类完全不变
    // 4. 所有方法逻辑保持原样
    // 5. 仅移除 ? 可空注解
}
```

**重要**：除了语法兼容性调整外，不对任何业务逻辑进行修改。

#### 2.2 所有服务类的迁移原则

**统一迁移规则**：
1. **添加显式 using 语句**
2. **移除所有 `?` 可空注解**
3. **保持所有方法逻辑完全不变**
4. **保持所有类结构不变**
5. **保持所有异常处理逻辑不变**

**示例调整**：
```csharp
// 原代码：string? result = null;
// 改为：string result = null;

// 原代码：public string? RecognizeTable(string? imageBase64)
// 改为：public string RecognizeTable(string imageBase64)

// 其他所有逻辑保持完全一致
```

#### 2.3 文件迁移清单

**需要迁移的文件**（按原样复制，仅做语法调整）：

1. **核心文件**：
   - Program.cs
   - TencentOcrService.cs
   - TencentOcrParser.cs
   - KeyService.cs
   - PathService.cs
   - GetFileContentAsBase64Service.cs
   - Logger.cs
   - HolidayService.cs

2. **模型文件**：
   - OcrResult.cs
   - OcrServiceException.cs
   - PathMessage.cs
   - KEY.cs

3. **接口文件**：
   - 所有 I*.cs 文件

4. **测试文件**：
   - HolidayServiceTest.cs
   - LoggerTest.cs

5. **配置文件**：
   - default.json
   - key.json

**迁移规则**：每个文件只做最小化的语法调整，不改变任何业务逻辑。

### 第三阶段：依赖包处理

#### 3.1 保持原有依赖包

**原则**：尽量使用与原项目相同的包版本，仅在必要时降级

**包版本策略**：
- **Newtonsoft.Json**: 保持 13.0.3
- **NPOI**: 保持 2.7.1（如果兼容）
- **Microsoft.Extensions.DependencyInjection**: 降级到 3.1.32
- **Nager.Date**: 保持 2.8.3

#### 3.2 依赖注入保持不变

**重要**：保持原有的依赖注入配置完全不变，包括：
- ServiceCollection 的配置方式
- 服务注册的方式
- 工厂模式的使用

#### 3.3 包兼容性测试

如果某个包不兼容 .NET Framework 4.7.2：
1. **优先尝试降级版本**
2. **其次寻找兼容的替代包**
3. **最后才考虑修改代码逻辑**

### 第四阶段：配置文件处理

#### 4.1 App.config 配置

创建基本的 `App.config` 文件：

```xml
<?xml version="1.0" encoding="utf-8"?>
<configuration>
  <startup>
    <supportedRuntime version="v4.0" sku=".NETFramework,Version=v4.7.2" />
  </startup>
  
  <!-- 仅在出现版本冲突时添加绑定重定向 -->
</configuration>
```

#### 4.2 配置文件直接复制

**完全按原样复制**：
- `default.json`
- `key.json`
- 所有其他配置文件

**不做任何修改**，保持配置文件的内容和格式完全一致。

### 第五阶段：功能验证

#### 5.1 基本功能测试

**仅验证功能是否正常**：
- [ ] 程序能否正常启动
- [ ] OCR服务是否可以调用
- [ ] Word文档是否能正常生成
- [ ] 日志是否正常记录
- [ ] 配置文件是否正常读取

#### 5.2 对比测试

**与原 .NET 8.0 版本对比**：
- [ ] 输出结果是否完全一致
- [ ] 处理流程是否相同
- [ ] 错误处理是否一致

#### 5.3 简单验证

**不进行复杂的性能测试**，仅确保：
- 功能正常
- 无明显错误
- 结果一致

## ⚠️ 注意事项

### 迁移原则

1. **最小化修改**
   - 仅做必要的语法调整
   - 不优化任何代码逻辑
   - 不重构任何结构

2. **保持一致性**
   - 所有功能保持完全一致
   - 所有配置保持不变
   - 所有依赖尽量保持相同版本

3. **简单验证**
   - 仅验证功能正常
   - 不进行性能优化
   - 不添加新功能

## 📋 迁移检查清单

### 项目配置检查
- [ ] 创建 .NET Framework 4.7.2 项目文件
- [ ] 配置 packages.config
- [ ] 设置正确的目标框架
- [ ] 添加必要的引用

### 代码迁移检查
- [ ] 添加显式 using 语句
- [ ] 移除可空引用类型注解（`?`）
- [ ] 添加命名空间声明
- [ ] 保持所有原有逻辑不变

### 依赖包检查
- [ ] 验证核心 NuGet 包兼容性
- [ ] 仅在必要时降级包版本
- [ ] 保持依赖注入配置不变

### 基本功能验证
- [ ] 程序正常启动
- [ ] OCR 服务调用正常
- [ ] Word 文档生成功能
- [ ] 日志记录功能
- [ ] 配置文件读取正常

### 一致性检查
- [ ] 输出结果与原版本一致
- [ ] 处理流程完全相同
- [ ] 错误处理行为一致

## 🚀 实施建议

### 开发环境准备

1. **Visual Studio 配置**
   - 确保安装 .NET Framework 4.7.2 开发包
   - 配置 NuGet 包源

2. **版本控制策略**
   - 创建新的分支进行迁移
   - 保留原始 .NET 8.0 版本作为参考

### 简化实施流程

#### 第1天：项目结构迁移
- 创建 .NET Framework 项目文件
- 配置 packages.config
- 复制所有源代码文件

#### 第2-3天：代码语法调整
- 批量添加 using 语句
- 批量移除可空注解
- 添加命名空间声明

#### 第4天：功能验证
- 编译项目
- 基本功能测试
- 与原版本对比验证

### 风险控制

1. **最小化风险**
   - 不修改业务逻辑
   - 保持原有架构不变
   - 仅做必要的语法调整

2. **快速验证**
   - 重点测试核心功能
   - 对比输出结果
   - 确保无编译错误

### 时间估算

- **第一阶段（项目配置）**: 0.5天
- **第二阶段（代码迁移）**: 1-2天
- **第三阶段（依赖处理）**: 0.5天
- **第四阶段（配置文件）**: 0.5天
- **第五阶段（功能验证）**: 1天

**总计**: 3-4个工作日

### 成功标准

1. **功能一致性**: 与原 .NET 8.0 版本功能完全一致
2. **结果一致性**: 处理结果与原版本完全相同
3. **正常运行**: 在 .NET Framework 4.7.2 环境下正常运行
4. **无语法错误**: 编译和运行无错误

## 📝 总结

本迁移文档专注于**最小化修改**的迁移策略，确保：

- **代码逻辑完全保持不变**
- **仅进行必要的语法兼容性调整**
- **快速完成迁移，降低风险**
- **保证功能与原版本完全一致**

通过这种简化的迁移方式，可以在最短时间内完成框架迁移，同时最大程度地保持代码的稳定性和一致性。

## 📞 技术支持

如果在迁移过程中遇到问题，建议：

1. **查阅官方文档**: Microsoft .NET Framework 迁移指南
2. **社区支持**: Stack Overflow、GitHub Issues
3. **版本兼容性**: 查看各依赖包的兼容性矩阵
4. **性能分析**: 使用性能分析工具对比

---

**文档版本**: 1.0  
**创建日期**: 2024年  
**最后更新**: 2024年  
**维护者**: 开发团队